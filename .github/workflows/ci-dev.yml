name: CI (dev)

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  python-services:
    uses: ./.github/workflows/python-services-reusable.yml

  flutter-apps:
    uses: ./.github/workflows/flutter-apps-reusable.yml

  auth-bot:
    uses: ./.github/workflows/auth-bot-reusable.yml
