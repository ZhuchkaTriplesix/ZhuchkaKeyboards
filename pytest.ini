[tool:pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --strict-config
    --tb=short
    --durations=10
markers =
    unit: marks tests as unit tests (no external dependencies)
    integration: marks tests as integration tests (requires containers)
    performance: marks tests as performance tests (high RPS and load testing)
    slow: marks tests as slow running
asyncio_mode = auto
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
